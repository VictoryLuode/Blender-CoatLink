hotfix-backups/
__pycache__/
*.pyc

# git-bash / MSYS leaves these behind while the shell tests run
.msys*

# release archives built by package.sh
dist/

# local-only files: never published, whatever they are called
AGENTS.md
CLAUDE.md
.cursorrules
.cursor/
docs/handoffs/
# local scratch (plans, drafts)
.scratch/
